export type Option = {
  label: string;
  value: string;
};

export type NumberOption = {
  label: string;
  value: number;
};

export type Product = {
  id: number;
  title: string;
  description: string;
  price: number;
  image: string;
};
